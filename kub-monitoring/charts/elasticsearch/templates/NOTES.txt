Elasticsearch has been deployed to your cluster!

Elasticsearch URL: http://{{ include "elasticsearch.fullname" . }}:{{ .Values.service.port }}

{{- if .Values.ingress.enabled }}
Elasticsearch can be accessed via Ingress:
{{- range .Values.ingress.hosts }}
  http{{ if $.Values.ingress.tls }}s{{ end }}://{{ .host }}
{{- end }}
{{- else }}
To access Elasticsearch from outside the cluster, run:
  kubectl port-forward --namespace {{ .Values.namespace }} svc/{{ include "elasticsearch.fullname" . }} {{ .Values.service.port }}:{{ .Values.service.port }}
{{- end }}

For troubleshooting, check pod logs:
  kubectl logs -f -n {{ .Values.namespace }} -l "app.kubernetes.io/name={{ include "elasticsearch.name" . }}"